# surya 1.0 RC, 2026-09-06

Built overnight 2026-09-05 06:10 to 2026-09-06 by nine agent seats on two accounts, coordinated by jag-0905-raven under the owner's 06:17 autonomy order.
Decision 22 is the shape: a fork of comet, native GPUI, with haktui's browser, editor and tree.

## What you can do today

| Surface | State | Proof |
|---|---|---|
| Linux engine as a service | `deploy/install-engine.sh`, systemd user unit, token, tailnet bind | PR #10, `surya-engine token` prints the token |
| Windows app | zip in `/store/surya-dist/`, `surya.cmd`, Servers page with token | PR #3, #10, real Claude turn streamed from dtry |
| Version guard | amber banner when engine and app builds differ | PR #38 |
| Chat with Claude Code | comet's engine, stream-json | upstream |
| Agent-drawn cards (A2UI) | native renderer, 11 components, hardened against hostile cards | PR #7, #12, #17, #18, #24, #31 |
| MCP tools for agents | `surya-mcp`: show_card, send_message, list_cards, task board tools | PR #4, #6, #35 |
| Agent mail | send, drain, ack; envelope the model can trust; requeue cap | PR #1 |
| Agent states, needs-you inbox, always-allow rules | engine + inbox views | PR #9, #29, #42 |
| Files: tree + editor over engine RPCs | jailed to the checkout | PR #5, #11 |
| Task board | data, RPCs, MCP tools, pane | PR #6, #15, #23 |
| Browser pane (CEF) | behind cargo feature `browser`; Linux proven | PR #19 |
| The look | surya-light / surya-dark, floating cards, motion modes | PR #2, #36 |
| Shell | one right pane: Browser, Files, Tasks; rail entries; page title | PR #30, #40 |
| CI | headless tests + whole-workspace check + smoke on every PR | PR #8, #37 |

## How to run it

See `docs/quickstart.md`. In one line each: install the engine on the Linux box with the script, copy the token it prints, unzip the Windows build, add the server in Settings, start a chat.

## Known issues, in the order you will hit them

1. Windows renderer drops filled quads per frame (buttons, underlines, checkbox fills flicker). `docs/probes/windows-dropped-quads-2026-09-05.md`. Not the theme; needs a Windows debug bisect.
2. The browser pane is Linux-proven only; the CEF crate is excluded from CI (1.4 GB artifact), so a nightly job with CEF cached is owed.
3. The rename zeron -> surya lands last (decision 23, `scripts/rename-apply.sh`, PR #41). Until then the binary is still `zeron` and env vars are `ZERON_*`.
4. Pending permissions are lost on engine restart; a deleted chat denies its pending tool (by design).
5. Mac build parked (owner 06:08).
6. `cargo test` on Windows still fails: test-only unix code (source_control.rs tests, 13 files under engine/tests and harness/tests). `cargo build` is clean once PR #43 lands.
7. Never launch Xorg or any long-lived process from inside a `flock` block: the child inherits the lock fd and holds the lock for life (cost 40 min tonight). Use a lock-free shell or `flock -o`.
8. Agent mail ack race (found 11:23 on main's CI): a turn that completes before the engine records its run id loses that mail's ack. Fix PR fix/agent-mail-ack-race; if RC1 ships without it, a lost ack shows in `zeron mail drain` and the mail is not redelivered.
9. Test flakes in comet's own suites seen on CI tonight: harness/tests/claude.rs fixture chmod race (ETXTBSY), harness/tests/acp.rs steering order, engine empty_reasoning_deltas_are_heartbeats under load. Re-run once before believing a red.

## Where the record is

- `docs/handoff-2026-09-05.md`: the minute-by-minute log, every merge, hold and ruling.
- `docs/decisions.md`: decisions 1 to 23.
- `docs/critique/`: the design critique rounds with real frames.
- `/tmp/surya-*-handoff.md` on pc-ajim: each seat's own handoff.
