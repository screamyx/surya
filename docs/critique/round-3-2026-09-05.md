# Design critique, round 3: acceptance of the round-2 fixes

2026-09-05 11:40, seat surya-tasks, persona `.claude/agents/design-critic.md`, judged against decision 22, `docs/design-brief.md` and the round-2 findings in `docs/critique/round-2-2026-09-05.md`.
Rendered from main `4737c75`, `--features browser` build.
Every fix PR that answered round 2 is inside that base: #36 (T1-T3), #30 (L1, L3, L4), #31 (C1-C4), #29, #40.

## What was rendered

`app/scripts/critic-shots.sh`, same rig as round 2, four runs (shell, panes, browser, cards), 60 s settle per frame because the box was swapping (coordinator 11:06: "give the app 60 s before the grab").
The rig now opens a pane through the shell's own `SURYA_OPEN_PANE=files|tasks|browser` knob; the round-2 `SURYA_OPEN_BROWSER` variable is read by nothing on main since #30, which is why an earlier browser frame was a plain shell.

| Frame | colours | panics | File |
| --- | --- | --- | --- |
| Shell light 1440x900 | 13249 | 0 | `shots/round-3-shell-light-1440x900.png` |
| Shell dark 1440x900 | 13089 | 0 | `shots/round-3-shell-dark-1440x900.png` |
| Shell light 1100x700 | 9916 | 0 | `shots/round-3-shell-light-1100x700.png` |
| Shell dark 1100x700 | 9687 | 0 | `shots/round-3-shell-dark-1100x700.png` |
| Files pane light / dark | 15902 / 15769 | 0 | `shots/round-3-shell-files-{light,dark}.png` |
| Tasks pane light / dark | 17070 / 16965 | 0 | `shots/round-3-shell-tasks-{light,dark}.png` |
| Browser pane light | 13249 | 0 | `shots/round-3-shell-browser-light.png` (plain shell, see B1) |
| Cards light / dark 1440x900 | 15411 / 15136 | 0 | `shots/round-3-cards-{light,dark}-1440x900.png` |

Counters: `seeded=1 space=1 chat=1 run=1 tasks=4` per run, `shot=1 panics=0` per frame, 16 frames grabbed across the four runs, 0 black (a frame with 2 colours is a dead display, none this round).
Still not judged: clicks (no pointer tool on `:7`), phone width (window floor 900x600), motion.

## Round-2 findings, verdict

Sampled from `round-3-shell-dark-1440x900.png` with PIL: canvas `#0b0a08`, rail panel `#332e26`, feed panel `#332e26`, selected session row `#47433c`.

| # | Round-2 finding | Verdict | Evidence |
| --- | --- | --- | --- |
| T1 | Dark panel does not separate from the canvas | **Accepted** | Two panels of `#332e26` on a `#0b0a08` canvas read as two cards, at both sizes. `builtins.rs` now says: "1.47 is a 9.2x step in raw luminance and reads as two cards." |
| T2 | Accent on list markers and inline code | **Accepted where visible** | `render.rs:378`: "the one loud colour on numbering (design critic round 2, T2)", markers `text_muted`; `inline_code_text` returns `theme.text`. The seeded transcript's tail shows code blocks in neutral syntax colours, no accent. The list-marker and inline-code lines scrolled out of frame, so they are judged from code, not pixels. |
| T3 | Two greys for device line and branch line | **Accepted** | Session row crop: "surya @ devbox" and "docs/critique-round-3" in one tone, light and dark. `shell.rs:4586`: "One tone for every secondary line in the row". |
| L1 | No rail entries (decision 11) | **Accepted** | Rail card: Home, Needs you, Agents, Tasks, Files, then "All projects" and the session list. Home selected in the wash. |
| L2 | Transcript measure 736 px, 85-90 char lines | **Still open** | `transcript.rs:75` still `MAX_CONTENT_WIDTH: f32 = 736.0`. `docs/wave3-shell-wiring.md:96` parks it: "Owner call: 640 px at 14 px body, or 680 px at 15 px body. Left to the theme seat". |
| L3 | No page-title tier | **Accepted, one nit (N1)** | Title row in the feed panel: "Wire the Tasks pane into the shell" at 28 px semibold, sub-line "surya · docs/critique-round-3". The tier exists; the typeface is not the brief's. |
| L4 | 600 px of empty rail card | **Accepted** | The rail card ends under the entries; the session card sizes to its one row. |
| C1-C4 | Cards: accent code, cream bars, split metric, 24 px H1 | see Cards below | |

## Panes wired since round 2

| Screen | Verdict | Notes |
| --- | --- | --- |
| Files pane, light and dark | **accept** | Tree with git marks ("app M" in the accent, "docs ?" muted), filter field, empty viewer "No file / Select a file in the tree". Quiet, reads as a tool. |
| Tasks pane, light and dark | **accept, one nit (N3)** | Header "surya · 4 tasks" with the helper line "Agents pull from Queued when they go idle.", three columns, owner chips with the state dot, accent only on the add button and the running bar. |
| Browser pane | **not rendered** | See B1. |

## New findings, by owner

Theme (surya-theme):

| # | Severity | Finding | Evidence | Fix |
| --- | --- | --- | --- | --- |
| N1 | Major | The page title is Geist sans, the brief wants the serif at this tier | `docs/design-brief.md:61`: "Newsreader Variable (serif) \| Page titles only"; `surya.rs:248` `DISPLAY` is a size and weight only; `crates/ui/assets/fonts/` holds Geist and GeistMono, no serif face | Bundle Newsreader (or the chosen serif), give `DISPLAY` a family, use it in `render_page_title` and the Tasks pane header only |

Shell (surya-files):

| # | Severity | Finding | Evidence | Fix |
| --- | --- | --- | --- | --- |
| N2 | Minor | The chat title now appears three times in one view: titlebar strip, page title, session row | `round-3-shell-light-1440x900.png` top | Drop the title text from the titlebar strip; keep the device there, or nothing |

Browser pane (surya-cef):

| # | Severity | Finding | Evidence | Fix |
| --- | --- | --- | --- | --- |
| B1 | Critical | `SURYA_OPEN_PANE=browser` never shows the pane at launch, so the browser cannot be captured or demoed from a cold start | `round-3-shell-browser-light.log`: "browser: created id=1 (#1)", "browser: load_end status=200", then `visible=0 frames=0 size=0x0` on every 5 s line to t=65s; the frame is byte-identical in size to the plain shell (93193). `shell.rs:7991` treats "browser" as ready on the first render (`"browser" => true`) while Files and Tasks wait for `current_space`; `panel_key` (`shell.rs:1842`) is built from the active chat or selected space, both empty on that first render, so the Browser tab and the open flag attach to an empty key and are gone once the saved chat restores | Use the same readiness as Files and Tasks (a current space), or run the knob after settings restore |

Tasks pane (surya-tasks, mine):

| # | Severity | Finding | Evidence | Fix |
| --- | --- | --- | --- | --- |
| N3 | Minor | At the default pane width the third column is cut at "Don" with no scroll hint | `round-3-shell-tasks-light.png` right edge | Three columns share the pane width (min 200 px each) before the board scrolls sideways; or show the overflow edge |

## Cards (C1-C4, PR #31)

Frames `round-3-cards-{light,dark}-1440x900.png`, colours 15411 / 15136, `panics=0`. The transcript tail shows the approval card's buttons, the whole diff card and the metric card; cards 1-3 scrolled out of frame and are judged from fixture and code.
Sampled from the dark frame: Thursday bar `#faf7f1` (`theme.text`), Friday bar `#c0b6a6` (`muted`), active tab underline `#fbf8f2`.

| # | Round-2 finding | Verdict | Evidence |
| --- | --- | --- | --- |
| C1 | Code and file paths painted in the accent tint | **Accepted** | Diff card: three paths in mono on a neutral wash, the code line above "Approve" in the body tone. No orange in either frame. |
| C2 | Metric bars in `theme.text` outshine the number | **Accepted** | Six bars in `muted`, the highest (Thu) alone in `text`; "38" is the brightest glyph on the card again. |
| C3 | "34 days on the lot ... of 90" split across the row | **Accepted from fixture** | `01-vehicle.json:14`: `"${/days} of 90 days on the lot"`, one caption. The vehicle card was out of frame. |
| C4 | 24 px H1 in a 14 px card, accent tab underline | **Accepted** | `leaf.rs:48`: `TextVariant::H1 => (20.0, 26.0, ...)`; underline sampled at `text`, not the accent. |

One question left for the theme seat, not a defect:

| # | Severity | Finding | Evidence | Fix |
| --- | --- | --- | --- | --- |
| N4 | Minor | With the accent gone from markers, code and tabs, no action wears it either: "Approve" and "Open PR" are solid `text` buttons, the accent lives only on the send button, the add-task button and the running dot | both card frames | Decide whether the primary card button is the one place the accent is spent (the brief: "One accent, spent only where an action matters"), or keep the dark disc and say so in the brief |

## What is actually good

- Dark mode now has planes: canvas, panel, selected row, each a real step. Decision 22's floating panels exist in both appearances.
- The rail reads as surya: five entries, then projects, then sessions.
- The Tasks board in the shell matches the standalone shot from #15: the same restraint with the accent.

## What I could not judge

- Interaction: nothing was clicked. The pane collapse, tab switching, the board's drag and the edit sheet stay unverified.
- The browser pane's URL bar, tab and page: the pane never showed (B1).
- Phone width and motion, as in round 2.
