# RC1 on Windows: e116422 built on dtry, run against the reinstalled engine

Date: 2026-09-05, 12:15 to 12:40 local. Seat surya-remote.

## What was built and installed

| Item | Result |
| --- | --- |
| Windows release build on dtry (`cargo build --release -p surya`, cargo 1.97.1 MSVC, every `.rs` touched first) | exit 0, 2m35s, exe 12:18:57 |
| Zip (`deploy/windows/build.ps1 -NoBuild -Sha e116422ff -CommitTime ...`) | `/store/surya-dist/surya-windows-e116422ff.zip`, 23752788 bytes, sha256 `6884c627c6b1b455...` |
| Linux release build through the throttle | exit 0, binary 12:28:16 |
| Service engine reinstalled (`deploy/install-engine.sh --no-build`) | unit active, pid 870632, `surya status` prints `Build:    e116422ff (0.2.34, 2026-09-05 04:15 UTC)`, token unchanged |

## Shots (native captures cropped to the app window, all looked at)

| File | Shows |
| --- | --- |
| `rc1-dtry-chat-a.png` | Dark. The chat turn from Windows: "reply with the single word mango and nothing else", answer "mango", row Done, 12:30 PM. |
| Servers page, dark, shot removed | "Add server" paints as a white plate. |
| `rc1-dtry-appearance-light.png` | Light. Appearance page: preview cards, all eight accent swatches, the Glass and Motion controls, "Add theme" plate, all painted. |
| Servers page, light, shot removed | "Add server" paints as a plate. |
| `rc1-dtry-chat-light.png` | Light. The same chat. |
| `rc1-dtry-cards-c.png` | Dark, embedded engine with `SURYA_DEMO_CARDS`: approval card "Approve" and diff card "Open PR" plates, metric card draws seven bars, "Break it down by salesperson" has its border. |
| `rc1-dtry-cards-d.png` | Same run scrolled up: form card "Save follow-up" plate, checkbox fill, field boxes; approval card again. |

## Counters

| Check | Counter |
| --- | --- |
| Chat turn from the Windows app over the tailnet, real chat, no demo knob | asked=1 delivered=1 |
| Primary plates visible across the shots (Save follow-up, Approve, Open PR, Add server dark, Add server light) | asked=5 painted=5 |
| Checkbox fill | asked=1 painted=1 |
| Metric bars | asked=7 painted=7 |
| Appearance preview cards / accent swatches | asked=3 painted=3 / asked=8 painted=8 |
| Dropped elements across the seven frames | asked=7 frames, dropped seen=0 |
| Version banner with app and engine both at e116422ff | asked=1 shown=0 (correct, same sha) |

This morning's build (`docs/probes/windows-dropped-quads-2026-09-05.md`) showed the same plates as dim text and the same fills missing. The difference is PR #56, the gpui fork repin with surya-cef's HLSL stride fix.

## Seen along the way

- With the engine given by `SURYA_ENGINE` (no saved Servers entry), the Servers page says "Connected to ws://100.83.77.3:27700 (ws://100.83.77.3:27700)" and puts the Active badge on "This computer". Cosmetic.
- The demo-cards knob now seeds once (`demo_cards_seeded`), so against a remote engine with sessions the fake chat is gone by the next chat-list frame; the card shots come from the embedded engine on a fresh data dir, where the list stays empty.
- The launcher's raise (`SetForegroundWindow` from a scheduled task) stops working after the first time; a TOPMOST then NOTOPMOST `SetWindowPos` toggle raises the window every time.
- The 09:29 message this morning that the app reported as "Not delivered" was in fact answered by the engine (the reply is in the session transcript at 09:29); only the app never adopted it, because the demo knob held the selection.
