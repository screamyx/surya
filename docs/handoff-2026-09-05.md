# Handoff, 2026-09-05 02:45, session jag-0905-raven

Read this first when picking surya back up.

## Where things stand

The repo is screamyx/surya, checkout /home/user/git/surya, branch main, everything pushed as of commit 03d6146.
`docs/decisions.md` holds 19 decisions with the owner's words quoted. Read it before advising on anything.
`mockup/` is the static mockup of surya 1.0 RC, twelve surfaces, stock shadcn base-nova on neutral, Bun, React 19, react-router 7, motion, CodeMirror.
It is the ground truth for the real build. Nothing of the real daemon or GUI exists yet.

## What is running on the box

- Vite dev server for the mockup on :4790, started with nohup from `mockup/`. Reachable as http://pc-ajim.tail82fec1.ts.net:4790/. Restart: `cd mockup && bun run dev`. Kill by port pid, never `pkill -f vite` (that matches your own shell).
- Gallery of screenshots on :4791, `python3 -m http.server` in /tmp/surya-gallery, pid in /tmp/surya-gallery/srv.pid. Serves /tmp/surya-gallery/index.html and shots/. The repo copy is mockup/shots/index.html.
- A lavish session for the A2UI explainer, artifact `.lavish/a2ui-cards.html`, URL http://pc-ajim.tail82fec1.ts.net:4387/session/77165dfb9222d9f7. A background poll (harness task bgb2wlkgk) waits for the owner's feedback. The page still shows a "vehicle" card in its lab; per decision 14 it should say "record" with workspace cards on top. Update it on the next owner feedback, or end the session with `lavish-axi end .lavish/a2ui-cards.html`.

## Builders: all closed 02:50

surya-mk3-servers reported at 02:47, seat closed and forgotten.
Its work is in commit a66b000: Servers panel in Settings, four-step Add server sheet, home grouped by server, New ask pre-filled from `?ws=`.
Gallery re-shot at 02:50, twelve surfaces incl. Messages, both sizes, commit 8ab561e.
The committed mockup/shots/index.html carries no host-specific link any more; the /tmp copy on :4791 still does.
Shoot script: /tmp/surya-shoot.sh (own CHROME_DEVTOOLS_AXI_PORT, prints asked=24 shot=24).

## Unslop round, 02:52 to 04:00

Owner: "it looks too ai-generated", then "the UX needs massive improvement".
Installed the ux-ui-agent-skills kit v2.5.1 at the repo root (AGENTS.md is the kit persona, CLAUDE.md shims it; `.claude/agents/design-critic.md`, `/critique`).
Seat `surya-unslop` (Opus 5 1M, xhigh, luvus pane 391) owns the round: baseline critique (reject), design brief (warm workshop, Claude anchor), token pass (round 1, rework), then four Sol researchers on twelve ADEs -> `docs/research/synthesis.md`.
Decision 20 (03:58) took all four synthesis changes and kept the look. The layout round is running now with Opus builders spawned by surya-unslop; it reports to jag-0905-raven with one REPORT when the critic says ship or after three rounds.
On that report: read the shots, `/critique` verdict file, commit, refresh /tmp/surya-gallery/shots, and bring the owner the gallery link.
Codex workers have windows-dtry since 03:05 (memory: codex-windows-dtry-mcp-wired). Sol ceiling is effort high, no fast mode (owner 03:10).

## THE PIVOT, 05:31 to 06:10: surya is a fork of comet, native GPUI

Read decision 22 first. The React mockup is frozen; `app/` is zeronsh/comet v0.2.34 as a subtree (compiles on this box in 3m47s, probe dir /store/surya-probe). Deadline: 1.0 RC by 2026-09-06 13:00.
Owner seat rules this round: "no sol, only claudes"; Fable id is `claude-fable-5-1`; Opus is `claude-opus-5[1m]` high; the Fable guard is suspended by the owner until the deadline (tools/quota-unlock/wizard.sh in project-jag). Spawns go through `jag-0904-crimson` (user uid) by agb order; workers report to jag-0905-raven.
Owner 06:09: "half builders here, half builders on user"; 06:11: Fable low/medium is fine for ordinary builders.
Live seats at 06:14 (nine): user uid via crimson: surya-cef 237, surya-a2ui 239 (Fable xhigh), surya-mcp 241, surya-mail 243, surya-theme 245 (Opus high). user2 via raven: surya-remote 429 (Fable xhigh), surya-states 431 (Opus high), surya-tasks 433, surya-files 435 (Fable medium). Briefs /tmp/surya-{remote,states,tasks,files}.md.
Earlier live-seat line, superseded: Live seats at 06:10: surya-cef 237 (Fable, CEF pane, 6h box), surya-a2ui 239 (Fable, A2UI renderer, 8h), surya-mcp 241, surya-mail 243, surya-theme 245 (Opus, 6-8h). Briefs: /tmp/surya-fork-common.md, /tmp/surya-{cef,a2ui,mcp,mail,theme}.md. Owner 06:06: CEF seat may swap to Zed's gpui; glass features go behind a flag.
Plan and cut lines: the task board in this session (TaskList) and the "high-level build plan" message at 06:03: wave 2 = inbox spine, editor+tree, task board, cards wiring; wave 3 = testers, critic, rename zeron->surya; wave 4 = packaging, root CI, docs. Cut order: iOS, multi-server, task board, light theme, pins, browser pane.
On a seat REPORT: read its PR, merge to main if green (coordinator merges, never the seat), close its pane via crimson or `luvus pane close`, `agb forget`, update the task, brief the next wave.

## STATE AT 06:20 (compaction point; owner asleep from 06:17, autonomous to the deadline)

Owner's last orders: "im going to sleep, so i need you to work autonomously. unblock any blockers, the most important thing is to ship this." His note "To the swarm" is at /tmp/surya-swarm-note.md and was sent to all nine seats at 06:18.
Nine seats live (see the 06:14 line). Reports and status lines land in jag-0905-raven as agb wakes; a session-only hourly cron sweep (xx:43) checks liveness, merges green PRs, nudges silent seats, spawns wave 2; one-shots at 12:17 (cut-line check), 22:07 (wave 3 start), 04:07 Sep 6 (wave 4 start). If this session dies, a successor must re-create those four crons from this paragraph.
Findings tonight for seats: comet compiles here in 3m47s; the headed app starts under xvfb but paints black (no DRI3), so visual proof is dtry session 1 or element-tree tests; codex/Sol are OFF this round; Fable id claude-fable-5-1.
Repo: main = 7c6cc75 docs + app/ subtree; every seat works on its own clone under /store/agent-worktrees/<seat> and opens a draft PR; the coordinator merges. No PRs open at 06:20. Task board: TaskList in this session (items 5 to 16).
Merge order when PRs land: mcp, states, tasks, files, mail (engine, additive modules) -> theme -> a2ui -> cef (may swap gpui) -> remote. Resolve one-line registration conflicts yourself.

## Next step

The build plan for the daemon and the GUI, with the mockup as the spec. Decisions 16 and 19 carry the probed facts the plan rests on. The owner's process for plans is /specops in project-jag terms, but surya has no plans/ folder yet; that is the first thing to decide.

## Gotchas learned tonight, all verified

- Any `claude -p` or `claude auth` run from an agb-registered session inherits AGB_AGENT_ID and re-registers it under the child's pid. When the child exits the wake route dies and mail piles up silently. Always run probes with `env -u AGB_AGENT_ID -u AGB_AGENT_RECAP -u AGB_RUNTIME -u CLAUDECODE`. Recover with `agb register --id <me> --recap "..." --runtime claude` from your own pane, then prove it with a self-send.
- chrome-devtools-axi: always set a distinct CHROME_DEVTOOLS_AXI_PORT per session (10100 to 10999 worked), or you silently drive another session's bridge. Screenshots to /store fail silently; use /tmp.
- Lavish hides any element with class `mermaid` until Mermaid marks it processed, and Mermaid could not run inside its frame. Pre-render to SVG under another class.
- Vite blocks unknown hostnames; `allowedHosts` in vite.config.ts carries the tailnet domain.
- The shadcn resizable wrapper is stale against react-resizable-panels v4 (orientation, pixel sizes). The files builder worked around it.
- Every file 500 lines max is the owner's rule. data.ts is split into data.ts, cards-data.ts, files-data.ts, settings-data.ts, slash-data.ts, all re-exported from `@/data`.
- Builders were spawned with `/home/user/.local/bin/spawn-agent <id> claude-opus-5 high <brief> /home/user/git/surya`, one at a time in the foreground, briefs in /tmp/surya-mk*.md with /tmp/surya-mk-common.md as the shared rules. Reports come back over agb.

## Headless Claude Code facts the daemon needs

See docs/probes/headless-controls-2026-09-05.md and docs/probes/agui-disconnect-2026-09-05.md. In one line each: runs outlive the browser but the runtime cannot cancel them; `--permission-prompt-tool stdio` is what makes permission asks and AskUserQuestion reach the host; `/model <name>` switches mid-session; `--resume <id>` survives a dead process; a control interrupt stops a running command in 2 seconds; Bun runs the whole stack.

### Context monitor, 06:25 (owner 06:23: "monitor the context window of each agents, especially the fables")
Two layers.
Seats self-checkpoint: at 40% left commit, push, write `/tmp/<seat>-handoff.md`, send raven CHECKPOINT, then `/compact`.
At 25% they send REPLACE and exit; the coordinator respawns the same id on a fresh session that reads the handoff first.
The rule is the last section of `/tmp/surya-fork-common.md`, sent to all nine seats at 06:24.
Backstop: `tools/ctxwatch/ctxwatch --watch --only surya- --floor 35 --interval 120 --notify jag-0905-raven` runs as a background task in raven's pane for the user2 seats, and crimson was ordered to run the same in its pane for the user seats.
On a ctxwatch alert raven sends the seat `CHECKPOINT NOW`.
Replacement of a user seat goes through crimson: close pane, `agb forget --dead-letter`, respawn same id, brief plus the handoff.
A successor session must restart raven's ctxwatch task; it dies with the session.

### xvfb finding, confirmed from the fork source (surya-cef 06:25)
"gpui_wgpu/src/wgpu_context.rs (e2ddcc6) lines 51-60 read ZED_DEVICE_ID as a PCI device id only" and "lines 312-318 skip every DeviceType::Cpu adapter because new() passes reject_software=true unconditionally (line 41)".
So no xvfb pixels without a one-line fork patch or a real X server on the NVIDIA card.
surya-cef adds `SURYA_BROWSER_DUMP=<dir>` to write CEF's own frames as PNG for proof; window screenshots stay on dtry.

### Quota monitor, 06:27 (owner 06:25: "also monitor the quota ok")
`tools/quotawatch/quotawatch` (project-jag v2, 98ea02e0) reads the 5h/7d footer off any Claude pane.
Raven runs `--watch --h5 80 --d7 70 --notify jag-0905-raven` as a background task for user2; crimson was ordered to run the same for user.
Reading at 06:26: user2 5h 29%, 7d 6%. Crimson's table at 06:24: cef 69%, a2ui 69%, mail 81%, mcp 81%, theme 78% context left.
Rule: past 80% of the 5-hour window on a uid, hold new spawns on that uid until the reset, running seats finish.
Both watch tasks die with raven's session; a successor restarts them.

### Headless Xorg on the NVIDIA card works (surya-cef 06:26)
"sudo Xorg :7 -config /tmp/xorg-nvidia-headless.conf -modulepath /usr/lib/x86_64-linux-gnu/nvidia/xorg,/usr/lib/xorg/modules -noreset -ac -novtswitch", log /tmp/xorg7.log shows "Initializing extension DRI3".
Any user: `DISPLAY=:7` plus `ffmpeg -f x11grab -video_size 1600x1000 -i :7 -frames:v 1 out.png`. Pixel proof pending.

Crimson 06:26: user side "5h went 7% at 06:09 to 16% at 06:26, about 9 points in 17 minutes with five seats working. At that rate 80% is roughly two hours out, before the 3h3m reset" (reset about 09:30). Crimson warns at 60%.
Plan: wave-2 spawns before 09:30 go on user2 (29% at 06:26, four seats); user side takes wave-2 seats after its reset.

### Two swarm findings, 06:27, pinned in the common brief and broadcast
surya-states: "as user2, cargo is NOT on PATH and there is no /home/user2/.cargo. A bare 'cargo check' dies with 'Failed to find executable cargo' and heavy swallows it as exit 0". Fix is RUSTUP_HOME=/home/user/.rustup, own CARGO_HOME on /store, PATH=/home/user/.cargo/bin. Seats must print `cargo --version` before a cited build.
surya-theme: llvmpipe on Xvfb still grabs black, "Vulkan WSI does not present into the Xvfb drawable at all". Pixel proof therefore goes to surya-cef's Xorg :7, not Xvfb and not dtry.

Correction 06:29 (surya-cef): on :7 "the zeron window maps ... but every grab is still black with only the X cursor". Suspect depth-32 ARGB visual with no compositor. :7 is not a proven pixel path. Seats told: proof = counters, unit tests, CEF frame dumps; only surya-cef works the screenshot problem, 20 min cap.

06:31 surya-cef: ":7 DEAD END, dropped" - depth-24 visual, Composite off, still pure black. "neither Xvfb nor a headless NVIDIA Xorg gives x11grab pixels for gpui on this box". Xorg :7 left running, harmless. Window screenshots of the app need dtry (wave 4); all Linux proof is counters, tests and CEF frame dumps.

### Build load, 06:34
`uptime`: "load average: 319.11", 16 cores, 74 rustc processes over nine seats, RAM 30 GB with 10 available, swap 23 GB fully used, memory pressure `some avg300=8.42`, no OOM kill in the kernel log.
surya-theme: "cargo check -p zeron-ui -p zeron-theme has been running 25+ min ... Nothing is compile-verified yet."
Rule broadcast to all nine and pinned in the common brief: `CARGO_BUILD_JOBS=3`, full binary builds under `flock /store/surya-zeron-build.lock`, per-crate check and test, one binary build per seat at the end.
Lock file is `/store/surya-zeron-build.lock`, mode 666, root-owned.
Sweep cron: check `uptime` and `free -g`; if swap stays full and available drops under 4 GB, order the two lowest-priority seats (tasks, files) to pause cargo until the load halves.

### OOM at 06:37, throttle installed 06:39
surya-theme: "box is at load 425, 9 GB available, 66 rustc. My 'cargo test -p zeron-ui -p zeron-theme --lib' was OOM-KILLED by the harness at 06:37".
Fix: `/store/surya-cargo` (root-owned, 755), a flock wrapper holding one of three swarm-wide slots (`/store/surya-cargo-slot-{1,2,3}`, mode 666) and running cargo with 4 jobs. Mandatory for every seat on both uids from 06:39; pinned in the common brief and broadcast.
At 06:39 the three user2 cargo processes (files 22 min, tasks 21 min, remote 5 min) had exited on their own, RAM available climbed to 18 GB, load 251 and falling. Their seats will say whether they finished or were killed.
`/store` root is not writable by user2; use sudo for files there.
surya-theme: draft PR #2 on feat/surya-look, "do not merge, build not green yet"; one compile error found and fixed (settings.rs:801 missing field motion).

06:40 surya-theme: "my Claude harness has its own low-memory watchdog that KILLS harness-tracked background tasks within seconds while the box is thrashing. It killed three of mine in a row at 06:37-06:39, including the /store/surya-cargo run". That is also why raven's three user2 cargo processes vanished. Recipe pinned in the common brief: every cargo run detached with `setsid nohup ... & disown` into a log in the worktree, waited on with a bounded `tail -f | grep -m1`. Raven's ctxwatch and quotawatch background tasks survived (checked 06:41).

### MERGE 1 of 9: PR #4 surya-mcp, 06:48, main 09e86a2
Opus reviewer (subagent, two passes, tip 001dcd6): "MERGE - all brief rules hold, merge is clean, no hazards found." Largest new file 481 lines; UI touched by one `surya: None` line; 24 tests in the crate; no cargo run by the reviewer, seat's own run is the compile evidence.
Follow-ups handed to surya-mcp on feat/cards-wiring: `env!("CARGO_BIN_EXE_surya-mcp")` in tests/stdio.rs, a test for `--disallowed-tools`.
surya-mcp re-assigned: C2 cards wiring engine half, brief /tmp/surya-cards-wiring.md, 3 h box. Task #13 in progress.
All other seats told to rebase on main.

### Memory, 06:55
Crimson 06:53: "Both background tasks were killed by my harness at 06:53 ... free -h at the time: 30Gi total, 433Mi free, swap 19Gi of 23Gi used"; restarted, two-minute hole. Cause = link-time peaks of three concurrent cargo slots plus 229 chrome processes. Raven killed 7 stale user2 chrome-devtools-axi bridges (pre-seat, from the mockup and research rounds): chrome 229 -> 145, swap 20 -> 17 GB, 20 GB available, load 7.8. Crimson asked to do the same for stale user-side axi bridges, never open-claude-in-chrome hosts.
Crimson's table 06:53: a2ui 62%, cef "no bar" (unmonitored that tick), theme 71%, mail 77%, mcp 73%. user 5h 23%.
surya-mail 06:53: mail GREEN on main-merged branch (engine lib 133 pass, agent_mail 3 pass), PR #1 head 2f26415, uses the MCP send_message delivery_id as the mail row id, final REPORT after the detached link.
surya-files 06:52 REPORT green: PR #5 draft, 6 tests, tree median 11.5 ms on 2003 files, watch edits=3 events=3, stale asked=1 refused=1, jail asked=7 refused=7. Opus review running. Seat re-assigned to the tree + editor views, brief /tmp/surya-files-ui.md, branch feat/files-ui, no shell.rs edits.

### MERGE 2 of 9: PR #5 file RPCs, 06:56, main d94782a
Opus reviewer: "VERDICT: MERGE. Two non-blocking nits", jail "sound" (lexical join, canonicalize nearest existing ancestor, starts_with canonical root; TOCTOU identical to the pre-existing uploads.rs jail), 1326 insertions 0 deletions, 8 files.
Nits handed to surya-files for feat/files-ui: spawn_blocking around files_watch::watch(), doc comment moved back onto file_search_root, the median<100ms tree assert behind an env flag.
Reviewer flag: raw notify channel is unbounded (files_watch.rs:34), batch channel bounded 64; watch it in wave 3 testing.

### Sweep 06:57
agb: 8 seats live; surya-mcp `not_live` with 1 pending (the MERGED go-ahead), crimson asked to read pane 241 and respawn the same id on /tmp/surya-cards-wiring.md if the pane is a bare shell.
user2 context: remote 70, files 80, tasks 80, states 73. Box: load 7.3, 21 GB available, swap 12 of 23.
PRs open: #1 mail (final REPORT owed after the detached link), #2 theme (not green by its own word), #3 remote (unit tests queued). Merged: #4, #5.
No halfway status is overdue yet (first marks 08:40). Wave-2 already running: cards wiring (mcp), files UI (files).
06:58 crimson: surya-mcp is alive, blocked in a `timeout 900 tail -f` on its build log, so agb shows not_live with mail pending until the tail returns; nothing lost. Common brief changed: tail bound 300 s, repeated, so seats stay reachable.

07:02 Both coordinators' watchers now run DETACHED (setsid nohup, logs /tmp/raven-ctxwatch.log, /tmp/raven-quotawatch.log, crimson's /tmp/crimson-*.log). The harness low-memory watchdog keys off `free` (1.7 GB) while `available` is 19 GB; the kernel has never OOM-killed (dmesg 0). Gotcha: `pkill -f <pattern>` matches the calling shell's own command line and exits it with 144; kill by pid from /proc/<pid>/cmdline.
Quota 07:02: user2 5h 42% (reset 1h57m), user 5h 24% (reset 2h28m). user2 burn about 25 points per hour: watch for the 80% alert around 08:30.

### Notify gap closed by cron, 07:05
agb refuses sends from detached processes on both uids (verbatim: "not_registered: saved session belongs to a different pane"), so no detached watcher can page. Watchers log only: /tmp/raven-*.log (user2 seats), /tmp/crimson-*.log (user seats, pane 249 "crimson-watch", 644). ctxwatch appends every 120 s, quotawatch every 300 s; a timestamp older than 3 min = dead watcher.
Raven cron fd07217a at :13 :33 :53 reads all four logs and acts (35% -> CHECKPOINT NOW, 25% -> REPLACE, quota 80% -> hold spawns on that uid, available < 4 GB -> pause tasks and files cargo). Crimson relays on its own turns as the second layer. A successor session must recreate this cron.
07:04 a2ui at 57%, about 1 point per 2 min; first seat expected to self-checkpoint near 07:35.
surya-mcp 07:03: scope split agreed with a2ui; harness half written (tool_use_id stamped on card records, read_card by tool_use_id, Card emitted after ToolResult); ruled: rebase onto feat/a2ui-renderer, PR after a2ui merges.

### MERGE 3 of 9: PR #3 remote engine, 07:12, main 01fd7de
surya-remote 07:08 REPORT: Windows app built on dtry dialed the Linux engine at 100.83.77.3:27700 with the token, a Claude Code session started from dtry ran on Linux and streamed back ("The working directory is /home/user2/Downloads/taildrop and the platform is linux"). Screenshot /store/agent-worktrees/surya-remote-target/proof/dtry-transcript.png. dial asked=4 accepted=3 rejected=1. Proof engine left running, pid 2291911, token in its data-engine/ipc-token, for the owner to try.
Opus reviewer: "MERGE, with 3 follow-ups". Auth in the websocket handshake callback, so no frame parsing before auth; constant-time compare; token file 0600; loopback without token preserved and tested; non-loopback bind auto-generates a token. One rename against the rule: serve_ipc moved to ipc::serve, shim kept.
Follow-ups on feat/packaging: attach_to_daemon never passes ipc_token (real bug, one line); ui-settings.json 0644 with plaintext token, set 0600; stale doc comment lib.rs:846.
surya-remote re-assigned: packaging, brief /tmp/surya-packaging.md (systemd unit + install script, Windows build + launcher + zip, owner quickstart).

07:28 surya-mcp REPORT on the cards harness half: green, feat/cards-wiring cc37658 on main, 206 tests pass, holds its PR until a2ui merges (rebase, drop temporary AgentEvent::Card). Third assignment while waiting: CI for the surya repo, brief /tmp/surya-ci.md, branch feat/ci, GitHub-hosted ubuntu-latest (never this box), check + headless tests, no fmt check.

07:31 surya-states STATUS: PR #9 draft (feat/agent-states), all four deliverables built, counters sessions=3 needs_you=2, asked=2 auto_allowed=1, children=1 rolled_up=1; one ui line added (transcript.rs:1291 folds the new MessagePart::Notice into the Text arm) so the workspace compiles, accepted. Full suite running, REPORT owed. Second assignment prepared: /tmp/surya-inbox-ui.md (needs-you list, agents-by-state rail, rules list, self-contained under crates/ui/src/inbox/).
07:31 surya-mcp: CI PR #8 open, run 33929792390 in flight; two ubuntu-latest jobs: headless tests for six crates, whole-workspace check with comet's release.yml apt list. Note: app/.github/workflows (comet's deploy/release/testflight) never fire here, GitHub reads workflows from the repo root only.
07:30 surya-a2ui: PR #7 draft, done except pixels; pixel proof deferred to dtry in the packaging round; opus review running. PR #6 (tasks) also under opus review.

### MERGE 4 of 9: PR #6 task board, 07:34, main 4ee8902
Opus reviewer: "VERDICT: MERGE", data model correct (tombstone delete, no resurrection on concurrent update), MutateParams internally tagged so additive, WatchTasks emits on every mutation, MCP status validated twice, no unwrap in production paths. Reviewer found the seat also fixed a real self-deadlock in publish(). Conflict warning: feat/agent-states touches the same proto/src/lib.rs hunk; surya-states told to merge main now.
Nits handed to surya-tasks for feat/tasks-ui: accept Null params on WatchTasks, drop the #[path] include of mcp/src/tasks.rs in engine tests, revert rustfmt churn in mcp/src/protocol.rs.
a2ui 07:32 REPORT result=partial (pixels only), accepted; next items for a2ui: second-reader review of mcp's cards-wiring PR, then end-to-end cards test after it merges.
cef idle at 59% since ~07:30 with no report; STATUS requested 07:34.

### MERGE 5 of 9: PR #7 A2UI renderer, 07:36, main 5a01d9b
Opus reviewer: "Verdict: MERGE ... with 3 fixes owed before RC, none blocking the merge itself." Proto and doc additive (name-tagged enums, Option + default fields, Loro round trip test present). Click path visible: the wire string appears as a user bubble before it reaches the agent.
THREE RC BLOCKERS handed to surya-a2ui on feat/a2ui-hardening (90 min box, before anything else):
1. Image component: "leaf.rs:74 ImageSource::from(url) -> ... img.rs:663 does an unconditional http_client.get ... A model-authored card fires an outbound GET to an attacker-chosen URL on render, no click"; file:// reads any local path. Fix: data: and workspace-local file:// only, remote behind a default-off setting.
2. "data.rs:93-99: updateDataModel with path /x/99999999999 hits while arr.len() <= index { arr.push(Null) } -> OOM." Cap the index.
3. No total node budget past MAX_DEPTH=24; add a per-card element counter and string truncation.
Nit: cards::log_render info-level + on_next_frame closure per render.
RC gate: the RC does not ship until feat/a2ui-hardening merges. Recorded on task #6.
surya-mcp released to rebase feat/cards-wiring onto main and open its PR.

### HOLD: PR #1 agent mail, 07:40
Opus reviewer: "HOLD - one hard compile break, one unwired deliverable, one replay bug." main.rs:223 arity (engine_config_from_env now takes Option<&str>), MailIngress never constructed, INSERT OR REPLACE clobbers on a repeated delivery_id. Design findings: ack not tied to the carrying run ("no redelivery path exists"), envelope body interpolated raw and `from` caller-controlled (impersonation at user trust level), rusqlite under a std Mutex from async, one global `delivering` lock across dispatch, #workspace with no match stores nothing silently.
Coordinator rulings sent to surya-mail: ack tied to run id with redelivery on errored/vanished run; envelope with indented body and closing marker; engine-set `from` for agent senders, `unverified:` prefix otherwise; spawn_blocking; per-agent lock; empty fan-out is an error. Out of scope noted. Fix round 90 min, smoke assignment waits.

07:48 surya-remote REPORT (packaging): green, PR #10 draft. Service engine installed on pc-ajim by deploy/install-engine.sh (systemd user unit active, pid 2542320, 100.83.77.3:27700, `surya-engine token` prints the token); Windows zip /store/surya-dist/surya-windows-463b8e3.zip launched on dtry via surya.cmd + servers.json, one Claude turn streamed; Settings -> Servers Add+Connect switched a running app. tests engine 133, rpc 13, ui 591. Follow-up: "'Add server' button on the Servers page renders dim (one-line style fix in ui)". FOR THE OWNER: on dtry the seat "accidentally restored a minimized PowerShell window and opened a Windows Terminal Settings tab", left as is, harmless.
Opus review of #10 running. surya-remote third assignment: /tmp/surya-dtry-proof.md (A2UI demo cards screenshots on dtry, docs-only PR, 45 min).
07:47 surya-files STATUS: feat/files-ui 6 files, tests green, draft PR opening. 07:48 surya-mcp: cards-wiring rebased on main incl #7, normalize.rs conflict resolved by layering store lookup, then a2ui input lift, then chip+result; suite running; CI run 33929792390 still in progress.

### MERGE 6: PR #10 packaging, 07:51
Opus reviewer: "MERGE." install-engine.sh idempotent, token to stdout only, unit binds from ZERON_BIND in ~/.config/surya/engine.env, no hardcoded IP; zip contains exe, dlls, surya.cmd, VERSION.txt only; all three PR #3 follow-ups verified. Optional follow-ups: token via env not argv in surya.cmd; SURYA_EXTRA_ARGS splitting; reshoot the two Servers screenshots (real tailnet IP as pixels) and the clipped chat shot.
Owner quickstart: docs/quickstart.md on main.

07:56 surya-a2ui REPORT hardening: green, PR #12 draft (feat/a2ui-hardening 5f2cca1): ImagePolicy (data: 2 MB image mimes, file only under chat cwd/fixture dir after canonicalize, http(s) = host-name placeholder, remote_allowed default off, no setting yet), index cap 10000, budget 5000 elements / 2000 components / text 4000 / labels 200, counters gated on ZERON_DEMO_CARDS or SURYA_CARD_STATS. hostile asked=4 parsed=4 index_refused=1 images_blocked=3. Opus review running; this is the RC gate.
07:56 PR #11 files UI review: "MERGE ... Two cheap fixes requested": load_tasks FIFO drain cancels listings on a truncated watch batch (key by dir), and the rpc.rs doc-comment nit from #5 still owed. Sent to surya-files; merge on its push. Noted: gitignored entries cannot be dimmed, the engine walker drops them (documented deviation).

### Sweep 07:58
agb: 9 seats live (mcp not_live = long log tail again). PRs open: #1 mail (fix round), #2 theme (silent since 06:40, STATUS requested), #8 CI (run in flight), #9 states (REPORT owed), #11 files UI (two pre-merge fixes owed), #12 a2ui hardening (RC gate, review running). Merged this sweep: #13 (a2ui dtry screenshots), #14 (docs/wave3-shell-wiring.md).
surya-remote 07:57: A2UI pixels on dtry, cards asked=6 seeded=5 rendered=5, button asked=1 fired=1. Visual defects routed: shared primary-button style bug (no pill, also hits Servers "Add server") -> surya-files fix/ui-nits; checkbox glyph, metric bars, first label offset, idle log spam -> surya-a2ui feat/a2ui-pixel-fixes after #12. surya-remote at ~42% ordered to checkpoint + /compact and stand by as the dtry hands.
GOTCHA: a review subagent left /home/user/git/surya on a detached HEAD (5f2cca1); reviewer prompts must say "do not checkout in the main checkout". Fixed with checkout main + ff.
Box: load 22, 20 GB available. No halfway mark overdue (first 08:40).

08:01 Seats cannot run /compact (slash commands are user-typed). Rule changed in the common brief: at 40% the seat checkpoints and stops at the prompt; the coordinator types `/compact` into the pane (`luvus pane send <pane> "/compact"` then a `\r`), sanctioned by the owner's 06:23 order. Done for surya-remote (pane 429, 44% left) at 08:01, result to confirm on the next read. Crimson told the same for user seats.
08:00 surya-theme interim REPORT: green, PR #2, full build 4m03s, theme 25 + ui 596 tests pass after merging main through #7; deliverable 5 (app screenshots) reassigned to surya-remote on dtry after merge. Opus review of #2 running, including a dry merge of feat/cef-pane on top.
Quota 08:00: user2 5h 64%, reset in 59 min; no new user2 spawns planned before the reset.

### MERGE: PR #11 files pane, 08:02, main 7144c2d
Two pre-merge fixes landed (load tasks keyed by dir, doc comment). 11 files, +1835. Merged so far: #4 #5 #3 #6 #7 #10 #13 #14 #11.
Crimson ruling 08:02: compact a user seat at <= 38% on the watcher reading when idle, without waiting for its CHECKPOINT; mail and theme stay open (fix round; review pending).
08:03 surya-files finding on the primary-button pixels: "light opaque fills fail to paint in the base layer on the dtry Windows release build and paint fine in the deferred layer" (Add-server dialog pill renders, page button does not; a2ui Primary is separate code with the same symptom). Points at the transparent-window path on Windows, not servers.rs or a2ui. Experiment queued for surya-remote after theme #2 merges (Opaque themes + window_background from theme). fix/ui-nits dropped for this item.

08:04 PR #2 theme review: "HOLD - one unblock only: the build is unverified" because the PR body still says not verified while the seat's 08:00 report says green. Everything else clean: default flip safe (stored themeSelection wins, resolve falls back only when absent), motion probe safe on Linux-without-gsettings and Windows, shell.rs 9 hunks layout-only with no signature changes, cef unaffected by this PR (its conflicts are with main itself: app/Cargo.toml, ui/Cargo.toml, ui/lib.rs). Asked theme for the PR-body evidence plus three nits (probe timeout and System-only, expect string, contrast test for canvas and card); merge on 'READY #2'. cef told to merge main now.

### MERGE: PR #12 a2ui hardening, 08:05, main 35295cb. RC GATE CLEARED.
Opus reviewer: "VERDICT: MERGE. All three PR #7 findings are genuinely closed." Image policy is on the only ImageSource path in the a2ui crate, remote_allowed reachable from nothing, canonicalize before a component-wise starts_with, data: length-gated before decode; index cap on both DataModel::set callers; budget taken per node with the panel replacing the whole body.
Follow-ups to surya-a2ui on feat/a2ui-pixel-fixes: memoize ImageDecision per (row, url) (policy rebuilt per frame today), a budget take per Tabs header, a render-path test for the self-reference panel, narrow the file root (chat cwd is ~ for project-less sessions).
08:05 surya-cef: origin/main merged into feat/cef-pane at cfc5d1a, three keep-both conflicts resolved, build 6 compiling as a systemd unit.
08:10 surya-states found a real cross-uid bug in the merged MCP harness glue: run_dir() is a fixed /tmp/surya-mcp/<agent_id>; owned by uid user, so user2's harness tests fail and one user can plant an mcp.json for the other. Sent to surya-mcp to fix on feat/cards-wiring (XDG_RUNTIME_DIR or per-uid dir 0700, prepare() takes the root, tests use a tempdir). Until then: harness suite fails as the second uid; not a seat's fault.

### MERGE: PR #8 CI, 08:11. CI IS LIVE.
Run 33930971315 SUCCESS in 12m19s on ubuntu-latest: "headless tests" (proto, doc, rpc, engine, harness, mcp) and "cargo check (whole workspace)" incl. gpui/ui with comet's apt list. First red run was the runner out of disk at link time ("ld terminated with signal 7 [Bus error]"); fixed with CARGO_PROFILE_*_DEBUG=0, --jobs 2, a free-disk step. "That is the first machine confirmation that main builds and its headless tests pass."
Merge rule from here: seat-green AND CI-green.
mcp: feat/cards-wiring rebased at 3ed07ff, suite rerun in flight after a self-inflicted stale-mix; one a2ui test changed to search for the Card instead of asserting ev[0] (card now rides the result frame).

08:20 Docs PR #16 merged (rename plan + dry-run script). Decision 23 written: rename last, copy not rename the data dir, ZERON_* aliases for one release, iOS/edge out of scope. PR #15 (tasks UI) opus MERGE, CI still pending at 08:20; merge on green. Harness watchdog killed the CI waiter again; check on wakes and ticks instead.
08:24 quotawatch read stale idle footers (user2 showed 56% for an hour; the live pane said 69%). Patched to prefer the newest footer clock (v2 45834e76), raven's copy restarted, crimson told. Real: user2 5h 69% reset 09:00, user 5h about 32% reset 09:50. a2ui at 42% idle, crimson compacts at 38.

### HOLD: PR #9 agent states, 08:25
Opus reviewer: HOLD. Security: "AllowRules::from_remember copies the client's remember.pattern verbatim with no check that it matches the answered command" (a '*' Global rule from any answer); "PermissionGate::ask fails open - ask(request).await.unwrap_or(Allow)" on chat drop; rules file 0644; env.sh with seat paths committed; agent_states.rs 646 lines. Design deviation: state ships on a separate WatchAgentStates stream, not on session rows; pending requests lost on restart; child nodes never pruned.
Coordinator rulings: separate stream accepted for RC (states is also the UI seat), restart loss accepted for RC, both noted as debt. Fix round 90 min, inbox UI paused.
08:26 PR #17 a2ui pixel fixes: HOLD for three small fixes (unconditional backslash replace corrupts Linux paths, BarChart charges one budget unit for 60 bars and has no height clamp, per-row image memo uncapped). Gap noted: CI's headless job does not run surya-a2ui or zeron-ui tests, only compiles them; a2ui to get surya-a2ui added.

### HOLD 2: PR #1 agent mail, 08:30
Re-review: all seven fixes present, five new holes: header forgery via `id`/`from` (raw at envelope.rs:76) and via ingress delivery_id; `\r`/U+2028 unsplit; `from` not engine-set because "RpcService::handle ... carries no connection identity"; requeue unbounded (permanently-errored agent loops, spawning a run each pass); lock map unbounded; publish_feed blocks SQLite on the reactor; socket and mail.jsonl default modes.
Ruling change: for the RC, fromChat must name an existing registry chat and the envelope renders its title; verified/unverified prefix dropped; "RPC lacks connection identity" recorded as debt. Requeue cap 3 then parked as failed. 64 sharded locks. Fix round 2, 60 min.

### MERGE: PR #15 tasks pane, 08:34, CI green + opus MERGE
08:33 surya-cef: REAL SCREENSHOTS work on this box: gpui on Xorg :7 renders one frame then never ticks; `DISPLAY=:7 xrefresh` (one Expose) wakes it; ffmpeg grab is a real 46 KB shot of the comet window. Recipe pinned in the common brief and broadcast; `/store/surya-display7.lock` serialises use of :7. Xorg :7 config /tmp/xorg-nvidia-headless.conf with Composite disabled. This unblocks the design-critic pass and theme screenshots locally.
08:31 a2ui READY #17 e07f27c (three fixes in, a2ui 26 + ui cards 4 + harness 1 tests); CI pending; merge on green.

08:33 surya-mcp REPORT cards wiring: PR #18 draft (feat/cards-wiring 200082c, rebased past #15), 240 tests pass, CI run 33933294279. Layered card resolution kept (store by tool_use_id, else input lift, else chip+result). Cross-uid run_dir bug fixed and found broader: the sidecar's config.rs read $UID (a shell builtin, absent in non-shell processes) so every user shared one /tmp/surya; both paths now use getuid, 0700, XDG_RUNTIME_DIR first. Opus review + a2ui second read running.
08:32 surya-files: first real screenshot of a surya pane on this box (files-demo on :7, tree with filter box, 20 rows, editor placeholder), going to docs/images via a docs PR.
08:36 PR #18 cards wiring opus review: "MERGE (with three notes, none blocking)": held chip lost on the crash/EOF path (Done bypasses the normalizer), read_card reads the whole store per card and nothing rotates it, cards.jsonl is 0644 (the 0700 applies to the run dir only). Merge on a2ui's second read + CI green. Follow-ups sent to surya-mcp.
08:37 a2ui second read of #18: MERGE ("fits the renderer"); one follow-up sent to mcp (configured store with no record yields an empty card; fall back to input only when no store). a2ui pixels on :7 for #17: asked=6 rendered=6, all six cards look right, "primary pills paint here (Linux), confirming the dtry pill bug is Windows-side". Shots go to docs/images via a docs PR. CI: #17 one job green one pending, #18 both pending.
08:38 Docs PR #22 merged (six A2UI card shots on :7). mcp folds [dead import, card-store 0700/0600, DirBuilder mode] into #18; behavioural follow-ups (crash-path flush, store-configured-no-record -> chip+result, subagent branch, rotation) go to feat/cards-wiring-2 after merge. Common brief: grep ^warning in your own new files before REPORT.

### HOLD: PR #19 CEF pane, 08:46
Opus reviewer: "HOLD - four blockers, all fixable ... The port itself is good work and the proof is real". Blockers: popup hijack (no on_before_popup, any new browser overwrites the single BROWSER static), nothing ever closes (hidden pane paints at 60 fps), zeron build.rs adds an RPATH unconditionally (default build changed), CI not yet green. Plus for RC: typed file:// and chrome:// URLs load; refuse them. Accepted-for-RC notes: crate excluded from the workspace so CI never compiles it (known rot), sandbox off (haktui parity), CDP bind address unverified, package-linux.sh does not ship CEF files, per-frame allocation, hand-rolled URL bar. libcef.so is 1.43 GB.
08:45 crimson compacted a2ui: 36% -> 100%, context kept. Footer clock lags ~10 s after a compact.
08:45 surya-tasks REPORT follow-ups: PR #23 (optimistic move frames=3 overlaid=2 confirmed=1, shared demo bootstrap, null params test), first tasks pane screenshot docs/shots/tasks-pane-2026-09-05.png; folding the quick-add nit before review.
08:47 PR #9 re-review: HOLD 2. (a) still open: "glob_match('*', anything) returns true ... so '*' passes the check trivially - and rules.rs:396-409 asserts exactly that". Ruling sent: literal prefix up to the first wildcard must be non-empty, a prefix of the command, and at least the length of its first token. Plus: DeleteChat never calls AgentStates::drop_chat; a Deny emits no transcript Notice; stale doc comment. 45 min.

### MERGE: PR #17 a2ui pixel fixes, 08:49, CI green
08:48 surya-mcp folded card-store 0600/0700 + DirBuilder mode + dead import into #18 (29ee7ed), 214 tests, warning-clean in touched files; CI rerunning. Warning census found a dead fn in mcp/src/tasks.rs:136 from PR #6, routed to surya-tasks. PR #9 CI green but code held on the wildcard rule. PR #23 CI pending, opus review running.

### Sweep 08:57. MERGE: PR #18 cards wiring, main 14ed5a3
9 seats live (mcp not_live = long tail). CI: #2 theme green (awaiting the seat's READY, silent since 08:00, STATUS asked), #9 states green (code held on the wildcard rule), #18 merged (opus MERGE + a2ui MERGE + CI green), #19 cef green (code held on four blockers), #23 tasks follow-ups pending (opus MERGE), #1 mail one job red on the pre-fix tip (seat told).
Released: surya-mcp -> feat/cards-wiring-2 (behavioural follow-ups); surya-a2ui -> feat/cards-e2e. Merged so far: #4 #5 #3 #6 #7 #10 #13 #14 #11 #12 #8 #16 #15 #20 #21 #22 #17 #18 = 18.
Box: load 8, 22 GB available. Quota user2 reset at 09:00.
09:01 surya-cef REPORT after fix round: PR #19 3da799f, CI green, four blockers claimed fixed, clean exit "helpers before=0 during=7 after=0 with 'shutdown closed=1'". Opus re-review running. Post-RC follow-ups named by the seat: Windows D3D11 + Mac IOSurface zero-copy, pins/CDP wiring, nightly CI with CEF cached.
09:00 surya-mail fix round 2 REPORT: head 2bc688b, all six rulings, engine lib 145; opus review 3 running.
08:58 rule: no rebase after a MERGE verdict while CI runs (tasks #23 reset its run three times).
09:01 crimson: user side 5h 42% reset 09:32; user2 reset at 09:00, hold lifted.
09:02 theme READY #2 385165e: three nits in, contrast test caught text_faint under target on both themes (seeds fixed), four real frames of the running app on :7 light+dark at two sizes in docs/design/shots (empty app, no project). Deviations accepted: canvas exempt from text_faint, body floor 11:1. Merge on CI green.
09:02 states fix round 2 REPORT: fc18e95, anchoring rule in (literal prefix trimmed of trailing whitespace must cover the first token), DeleteChat calls drop_chat, every Deny emits a Notice with reason, doc comment fixed. Opus review 3 running (probing edge cases); merge on review + CI.
09:02 a2ui e2e cards test: PR #24 (feat/cards-e2e, ui/src/cards_e2e.rs 232 lines), five hops all counted, 1 test green. Test-only; merge on CI.
09:04 PR #1 mail review 3: "MERGE. All six round-2 rulings land." Hostile chat titles cannot reach the header (falls back to chat id, then the charset check refuses). Follow-ups sent to surya-mail (create-with-mode instead of chmod-after, ingress env vars, validate `to`, docs). Merge on CI green.
09:05 PR #19 CEF re-review: "MERGE - the four blockers are fixed and verified", one line owed: the popup path bypasses the scheme allowlist (window.open('file://...')). Reviewer caveat: the browser crate is excluded from the workspace, so CI never compiles its ~4400 lines; green means "main still builds". Post-RC: a nightly job with CEF cached.
09:07 PR #9 review 3: "MERGE (2 non-blocking follow-ups)"; folding both before merge since they are in the same function: leading-whitespace off-by-n in the anchor (trim_start), and an empty 'remember' pattern stored as a glob instead of an escaped literal. Plus a Notice fold test. READY #9 awaited.

### MERGES: PR #2 theme (a139718), #23 tasks follow-ups, #24 cards e2e, 09:15. 21 merges.
09:14 states READY #9 a65b1bf: trim_start anchor, `exact: bool` flag on AllowRule (chosen over escaping: one place to agree instead of three), Notice fold tests. CI pending; merge on green.
PR #1 mail: "headless tests" job FAILED on 2bc688b, check passed; seat told to read the run and fix.
Released: surya-tasks -> design-critic pass on main via :7 (docs/critique/round-2-2026-09-05.md, findings routed per seat); surya-remote -> dtry round on main (Windows rebuild, pill test, quickstart reshoot). surya-files still holds for #19 (cef, one line + CI). theme, a2ui idle for critic fix rounds.
Quota: user2 reset, 5h 2%; user 5h about 42%, reset 09:32.

### MERGE: PR #9 agent states, 09:23. 22 merges.
Tick 09:22: lowest seat cef 43% idle (READY #19 owed since 09:05, STATUS asked); user2 5h 3%; user 5h 44% reset 09:32; 21 GB free; load 5.
09:24 MAIN RED on `cargo check --all-targets`: #9 added RunControls.permission, #24's cards_e2e.rs literal (merged after #9's CI ran) misses it. a2ui fixing on fix/cards-e2e-permission, one line. Lesson: two green PRs can be red together; after each merge the next PR's CI must rerun on the merged base before merge (GitHub runs PR CI on the merge commit only if the PR is rebased; ours were not). Rule from now: before merging a second approved PR within one CI cycle, wait for main's own push run to be green.
09:26 Found: every docs push to main cancelled main's in-progress CI run (concurrency group ci-main, cancel-in-progress), so main never completed a push run tonight. ci.yml now ignores docs/** and **.md pushes (5b6f318). Main's run on 5b6f318 will show the #9 + #24 red until a2ui's one-line fix merges.

### Critic round 2, 09:28 (PR #26 merged)
surya-tasks: 10 verdicts, 6 frames on :7; "the three send-backs: dark panels do not lift off the canvas, accent sprayed on list markers / inline code / paths / code blocks, no page-title tier and no decision-11 rail entries". Lists sent: theme T1-T3 (fix/critic-theme), a2ui C1-C4 (fix/critic-cards after #25), files L1-L4 folded into shell wiring phase 2 (GO given now, feat/shell-wiring, no wait for cef). Not judged: clicks, phone width, files card + browser pane, motion.
09:35 surya-mcp REPORT cards follow-ups: PR #28 (feat/cards-wiring-2 cc2038f), 220 tests, four items done plus a fifth found by its tests (no-store shorthand card rendered blank; lift gated on drawability, bare createSurface still draws). Opus review running; CI running (check job may hit main's E0063 until #25 merges).
#25 CI still pending at 09:35 (queued 10 min). Main run 33936279058 in progress.
