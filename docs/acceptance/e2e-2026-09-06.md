# Windows end-to-end acceptance, 2026-09-06

Tester: surya-e2e. Target: dtry, app and engine build `03ecccfe1`.
Input: native computer-use MCP. Isolated app data: `E:\surya-e2e-data`.
Gallery: http://pc-ajim.tail82fec1.ts.net:8613/e2e/.

Status: sweep recorded; acceptance is not green. Four groups pass, four fail,
and three remain incomplete because of approval or observation limits.
The isolated matching mock engine at pc-ajim:27710 and own Windows app are stopped.
Real-engine connectivity was observed before switching; no real-session screenshot was saved.
Counters count the eleven acceptance groups; incomplete groups are not failures. A group passes only when its required checks are covered.
asked=11 passed=4 failed=4 pending=3; design findings=1.

| Step | Expected | Observed | Result | Screenshot | Observation ID | Counters |
| --- | --- | --- | --- | --- | --- | --- |
| 01 Boot | Connect without skew; dark at 1440x900 and 1100x700; light/back and motion | No skew on real or isolated matching engine; dark at both sizes; light/back works; Reduced motion selected and persists after restart | PASS | `/store/surya-gallery/e2e/01-boot-dark-1440.png`, `01-boot-dark-1100.png`, `01-light-theme.png`, `01-motion-reduced.png`, `01-settings-persisted-1100.png` | `o_f2b3957144ad05e2` (persistence); `o_3e4aebf545953fda` (light) | asked=1 passed=1 |
| 02 Navigation | All rail pages and return paths; workspace and session create/rename/delete | Rail pages/back work except Agents design finding; new session, rename, archive/unarchive and two-project switch work; session delete approval pending | PENDING; FINDING (design) | `/store/surya-gallery/e2e/02-agents-no-effect.png` | `o_2f3f166d440b3141` | asked=1 passed=0 |
| 03 Chat | Stream, stop, retry, attach, model, send, transcript actions | Send/Enter, streaming, PNG staging/send, model/reasoning changes work; stop ends a 50-repeat paced run early; .txt silently discarded; user-turn clipboard copy verified; command disclosures expand; code clipboard copy verified; identical-prompt resend recovers a failed run; no dedicated Retry-run button observed | FAIL (E2E-CHAT-01) | `/store/surya-gallery/e2e/03-text-attachment-discarded.png`, `03-stopped-turn.png` | `o_9c4be1a9206174a9`, `o_d8613988487b58cd` | asked=1 passed=0 |
| 04 Needs you | Question and permission panels; answer outcomes, chip, collapse, badges, Open chat | Cold client restores inbox, confirming reconnect bug. Single/multiple-choice answers, Open chat, Allow, Deny and Always allow all work; chips and inbox collapse observed | FAIL (E2E-INBOX-01) | `/store/surya-gallery/e2e/04-inbox-misses-question-after-reconnect.png`, `04-question-panel.png` | `o_2b1195a7f8b8430a`, `o_3a52c3a765dce100` | asked=1 passed=0 |
| 05 Cards | Render A2UI, invoke button, transcript result | Approval card renders; Change it first and Approve add correct action/context to transcript and trigger reply | PASS | `/store/surya-gallery/e2e/05-card-render.png`, `05-card-action-result.png`, `05-card-approve-result.png` | `o_4a4c0e70d0b3a62c`, `o_e8d3c4bf85049d79` | asked=1 passed=1 |
| 06 Mail | Two test agents exchange mail | Warmed two mock agents, A sent to B; delivered and acknowledged; B transcript visibly contains envelope, sender and exact body | PASS | `/store/surya-gallery/e2e/06-mail-envelope.png` | `o_4284fb17c485e4c8` | asked=1 passed=1 |
| 07 Tasks | Create/status/reorder/delete; two-client synchronization | Quick-add cannot retain focus. Fixture task title/status edit, column move, same-column reorder and second-client synchronization work; approved delete removes A from board and second-client snapshot | FAIL (E2E-TASK-01) | `/store/surya-gallery/e2e/07-task-input-no-focus.png`, `07-task-second-client.png` | `o_b7985cd431437ba4`, `o_84f8337121f3bb95` | asked=1 passed=0 |
| 08 Files | Tree expand/open/edit/save/reopen; stale save refusal | Nested lazy expand/edit work. Default Ctrl+S toggles sidebar; dirty file switch loses edits. After sidebar remap, save/reopen and stale-hash refusal plus Reload from disk work | FAIL (E2E-FILES-01/02) | `/store/surya-gallery/e2e/08-save-shortcut-toggles-sidebar.png`, `08-file-switch-discards-edit.png`, `08-editor-saved-reopened.png`, `08-stale-save-refused.png` | `o_7556d6ddc0b41613`, `o_ca2b3c91caf3e990`, `o_b0d4f328df761a6c`, `o_3db6a89330ccf75f` | asked=1 passed=0 |
| 09 Browser | Page/address/tabs/find/zoom/history/reload/scroll; zero-copy pair; agent CDP | Native controls pass, last tab becomes blank; 200 find matches; real mock-agent MCP 4/4 and green screenshot card; zero-copy requested on also completes 4/4 with visible green screenshot card; GPU-path instrumentation and 120 Hz motion remain unverified | PENDING | `/store/surya-gallery/e2e/09-browser-agent-cdp.png`, `09-browser-find.png`, `09-browser-scroll.png`, `09-browser-close-last.png` | `o_f12dcec7a08588b2`, `o_018583566e88e8fa` | asked=1 passed=0 |
| 10 Settings | Every control has an observable effect and persists on restart | Device rename, server add, agent toggle, accent/glass/font, theme import/select and archive/unarchive work; shortcut persists; device/server/agent/accent/glass/font/theme/sound values persist after restart; invalid-server error and recovery work; deletion/notification approvals pending; accounts excluded | PENDING | `/store/surya-gallery/e2e/10-theme-import.png`, `10-shortcut-persisted.png`, `10-archived-session.png` | `o_6b93e358b8f0157c`, `o_5b6078a2fa364473`, `o_b7308c6cf0b670f6` | asked=1 passed=0 |
| 11 Shutdown | Own app and CEF helpers exit cleanly | Alt+F4 closes isolated app PID37600 and all five CEF helpers; fresh native windows empty and exact-folder process audit returns zero | PASS | `/store/surya-gallery/e2e/11-before-shutdown.png` | `windows(query="surya")=[]`; exact-folder CIM audit | asked=1 passed=1 |

## Setup evidence

- Required spawn smoke passed: shared-directory touch/remove, temporary worktree
  with real empty commit and clean removal, broker delivery.
- GUI slot granted by surya-cef3, 05:51 to 07:51 local; extended to 08:51; READY sent.
- Native `windows(query="surya")` connected to dtry session 1, app runtime
  `26.901.4073.0`, and returned no matching windows before launch.
- Stock `surya.cmd` hardcodes `%APPDATA%\surya`; it was not launched against the
  owner's data. Isolated launcher requested from surya-remote2.
- The brief's windows-dtry Screenshot tool is absent from this thread. Requested
  the documented session-1 capture fallback, now used for all saved frames.

## Findings

- E2E-NAV-01: Needs you -> Agents leaves Needs you selected and open.
  `app/crates/ui/src/shell.rs:4439` explicitly keeps the tree mounted and redraws.
  The clickable entry gives no visible result, contrary to the acceptance brief.
  Filed to jag-0906-osprey with screenshot and exact repro. At 06:05 the
  coordinator ruled this a nonblocking design finding: the tree belongs in the
  rail; the brief's separate-page expectation does not apply to Agents.

Setup limitations are not product failures.

## Execution notes

- Native `act launch` rejects `.cmd` launch targets. Used the provided session-1
  scheduled launcher; all app input remains native computer-use.
- Saved frames use the approved camera-only PrintWindow script and are opened
  after capture. Native screenshots before activation showed occluding content;
  those were not published. Pressing Escape brought the app forward.
- Added `/tmp/surya-e2e-mock/project` through the project picker: type path,
  Enter to enter folder, Ctrl+Enter to add. Settings opens via menu or Ctrl+comma.
- First own-window close left zero processes at the exact E2E exe path.

- E2E-CHAT-01: paperclip picker allows `.txt`; selecting the fixture closes the
  picker without attachment or error. `composer.rs:3788-3795` silently skips
  non-images. Coordinator confirmed RC bug and assigned surya-theme the fix.
- Native accessibility sometimes returns null even with a visible caret. One
  release/reconnect restored typing; another did not. Attachment tested through
  visible folder navigation and file double-click, without bypassing typing guards.

- E2E-INBOX-01: keep client open, restart isolated engine with question fixture,
  queue a test run. Session row says Input; RPC WatchSessions says awaitingInput;
  Needs you has no badge/rows and agent tree says No agents yet. Clicking the
  session then Home reveals the real pending question panel. Client-only restart restores the same pending rows and badge, confirming a
  reconnect subscription bug; assigned to surya-mcp by coordinator.

- Question single-select and multi-select submissions reflected the chosen answers
  in the transcript and cleared both inbox rows and badge. Evidence:
  `04-question-answered.png`, `04-question-inbox-cleared.png`.
- Allow produces Allowed, Deny produces Denied, and project-scoped Always allow
  produces Always allowed. Each clears the Needs you row and badge. Deny was
  restaged after its first five-minute fixture timeout; the completed second run
  is the action under test. Evidence `04-permission-allowed.png`,
  `04-permission-denied.png`; obs `o_b9ba73314d44f8c0`, `o_28f3b169aeb75e55`.

- Project permission rule repeat auto-allows, with visible `allowed by rule`
  transcript text; `04-permission-always-rule.png` inspected.
- Cards use built-in `ZERON_MOCK_CARDS` with repository approval fixture.
  Both buttons dispatch; no actual lead/database write occurs in mock harness.
- Mail fixture `mail-8281723f-5c00-42fc-af67-3606551b5fca` delivered and acked,
  with exact sender and body visible in B transcript, not merely RPC counters.

- Native typing opt-in validated after MCP restart: session rename succeeds with
  visible text selection and null UIA focus. `02-session-renamed.png`.
- E2E-TASK-01: single/double-click quick-add produces no retained caret; Tab
  does not enter it. `tasks/board.rs:381-383` root click focuses board after
  input. Osprey confirmed RC bug, assigned surya-mail. Edit-sheet input does
  retain caret and native typing works; title and Running status saved.
- Second RPC client sees UI-edited title/status. Its B title/status mutation
  appears immediately in the Windows board. A dragged Running->Queued below B,
  then above B, matching visual order and WatchTasks rank.

- E2E-FILES-01: default Ctrl+S toggles sidebar instead of saving; dirty dot
  remains and disk unchanged. Osprey confirmed RC bug, assigned cdp2.
- E2E-FILES-02: switching tree file clears dirty buffer without warning.
  Returning reloads disk and loses edit. Osprey confirmed RC data-loss bug,
  assigned browser-perf2.
- Settings remap sidebar to Ctrl+Shift+S enables real Ctrl+S save, disk and
  reopen confirmed. External write then save refuses with changed-on-disk banner;
  Reload from disk shows external text and clears dirty marker. Default still fails.
- Task delete approved by osprey, completed on own fixture A; before/after PNGs
  inspected, second RPC client confirms only B remains.
- /store briefly full during08-editor-saved-reopened copy; remote frame intact,
  retried copy succeeded and inspected. No missing evidence at this checkpoint.

- Browser fixture server is own port27720. Native index->two navigation,
  back/forward/reload, new/switch/close/last-close, find1/200->2/200,
  zoom100->110->100, and scroll to rows21-44 verified visually.
- Browser MCP proof uses matched03ecccfe1 app/engine and cdp2 sidecar a538456
  (newer sidecar provenance). Built-in mock_browser invokes real MCP subprocess
  open/snapshot/click/screenshot; tools_called=4 ok=4, card emitted=1,
  green two.html visible in both pane and transcript screenshot card.
  A later new-session fixture with pane hidden failed click (3/4); it is
  distinct from the successful visible-pane proof and not claimed as a pass.
- Settings Accounts detects host provider sessions outside the isolated data
  directory. No account mutation, authentication or saved account frame.
- Changed settings, subsequently confirmed after restart: device pc-ajim E2E, saved e2e.invalid
  server (later used for the controlled DNS-failure check), Codex disabled, green accent, opaque glass,
  System UI18px, imported E2E Navy dark theme, Sounds off/background-only off.
  Desktop notification toggle awaits approval; left-sidebar remap persists.
- New project-two added via native picker, selecting it empties session list;
  switching back restores original sessions. 02-workspace-switch.png inspected.

## Follow-up verification at 07:35 MYT

- Zero-copy-on launch completed the same visible-pane MCP sequence: four tools,
  four successes, fresh green screenshot card. `09-browser-zero-copy-on.png`
  inspected; observation `o_2bbccc4bc71c7ec0`. This proves functional behavior
  with the flag requested, not measured GPU-path use or 120 Hz smoothness.
  The target build's headed log is empty; no DevTools/performance counters claimed.
- Native resend of the identical failed prompt, with Browser visible, completes
  all four MCP operations and clears Run failed / Needs you indicators.
  `03-run-failed-no-retry-button.png` and `03-run-resend-recovered.png` inspected.
- Settings changes listed above survived restart. Appearance restored afterward
  to Zeron Dark, default accent/glass, Geist 16px; Reduced motion remains.
  Imported theme review shows four mapped tokens and zero validation warnings;
  duplicate-as-editable and reload controls work. Reveal, unlink and original-theme source linking were completed below; theme
  removal and account mutations remain unexercised.
- Connecting the own saved `e2e.invalid:27700` fixture produces DNS error11001.
  Fix server opens Settings; reconnecting own saved `100.83.77.3:27710` restores
  active state. `10-server-unreachable.png` is an inspected 680x200 crop.
  Neither the shared27700 server nor local owner engine was connected in this check.
- Session B delete, invalid-server removal and desktop-notification toggle were
  sent to delegated approver with exact visible text; all remain unanswered.
  The session Delete modal was cancelled while waiting. No pending modal remains.
- Shutdown: own app37600 and helpers45908,39376,46952,43332,31372 were all under
  `E:\surya-e2e-03ecccfe1`. Alt+F4 naturally removed all six; native windows
  returned empty and exact-folder CIM audit printed `own_process_count=0`.
  No forced termination was used for this shutdown proof.

## Additional Settings coverage at 07:43 MYT

- System appearance selected; alternate Surya Light palette selected and visibly
  applied in Light mode, then Dark restored. `10-surya-light-theme.png` inspected.
- Reveal opens own `E:\surya-e2e-data\custom-themes` in Explorer and selects the
  duplicate JSON. Explorer closed afterward. Unlink converts duplicate to an
  imported snapshot; theme and source file remain. Observation `o_bfc220d5ec346fd2`.
- Added separate `E:\surya-e2e-linked-theme.json` through Link to source.
  Selected E2E Linked, changed fixture colors externally, clicked Reload.
  Blue accent/preview changed to pink/purple immediately. Zeron Dark restored.
  `10-linked-theme-reload.png` inspected; observation `o_d4aa121716c40a93`.
- Claude Code turned off and persisted; Codex was already off. OpenCode initially
  stayed on because it was the last enabled installed agent (intentional guard,
  `settings/harnesses.rs:355-364`). Re-enabling Claude then permits OpenCode off. No real provider run or login was started.
- Individual shortcut Reset returns sidebar to Ctrl+S. Remap then Restore defaults
  does the same. Remapped again to Ctrl+Shift+S for the Files save workaround.
  Reset observations `o_4027147fb23f359e`, `o_3841df3ce9d40b86`.
- Additional approval requested for Remove on duplicate `surya-e2e-theme Copy`.
  Exact button text is Remove; source shows no confirmation dialog. Not clicked.
  The native skill's referenced confirmations policy requires action-time approval
  for deleting app data and subscribing/unsubscribing notifications. This is a
  tooling constraint, not a product failure; no native approval prompt appeared.

- Last-agent guard has no explanatory message on this screen; recorded as a
  coverage note, not a persistence failure. Initial OpenCode-off assumption was
  corrected after inspecting the unchanged switch and source.

## Final coverage and handoff

- The latest restart confirms Claude enabled, Codex and OpenCode disabled.
  `10-agent-settings-persisted.png`, observation `o_48790f012399fdbb`.
  Linked and unlinked library entries and Surya Light selection also persist.
  Sounds and background-only remain off; Desktop notifications remains on.
  `10-notification-settings-persisted.png`, observation `o_dde03542273148ba`.
- Four action-time requests remain queued to delegated approver jag-0906-osprey:

| Action on own fixture | Exact visible prompt/control | AGB request | Result |
| --- | --- | --- | --- |
| Delete session B | Delete session? “E2E Mail B renamed” will be permanently deleted. This can’t be undone. / Delete | `d74ae35a-5fb7-43f2-8d28-60ec328a3642` | APPROVAL PENDING; modal cancelled |
| Remove invalid saved server | Remove beside e2e.invalid:27700 | `e7c07967-001a-4aa7-9880-0f1f07eedb32` | APPROVAL PENDING |
| Disable desktop notifications | Desktop notifications | `5d6969ad-33ae-43e9-bdd9-0da0577a690c` | APPROVAL PENDING; stays on |
| Remove duplicated theme | Remove beside surya-e2e-theme Copy | `bf60b0f6-780f-4152-8f61-f5b832d2f1ca` | APPROVAL PENDING |

- Session confirmation captured in `02-session-delete-approval-pending.png`,
  observation `o_782185bb2e33a10c`; Cancel leaves session present. Task A's
  separate deletion was explicitly approved and completed earlier.
- These requests originate in the computer-use skill's referenced
  `references/upstream/docs/confirmations.md`: “Always Confirm at Action-Time
  (Even If Pre-Approved)”, covering app-data deletion and notification changes.
  They are not native runtime approval prompts. The arbiter confirmed broker
  delivery and requested that approval-pending rows not halt the other checks.
- Browser controls and both requested flag configurations work. 120 Hz motion,
  actual GPU-path use and DevTools counters are unverified with this screenshot
  workflow. Group09 therefore remains incomplete. Provider authentication,
  account changes and installation of absent CLIs were excluded. Shortcut binding
  behavior and palette choices were sampled; not every combination was exercised.
- Five confirmed RC bugs remain on tested build03ecccfe1: text attachment discard,
  Needs you reconnect, task quick-add focus, Ctrl+S conflict, dirty-buffer loss.
  All were filed when found; no newer matching Windows build was supplied for retest.
- Final Alt+F4: app14620 plus five helpers becomes zero naturally.
  `e2e-2026-09-06-processes-before.txt` and `e2e-2026-09-06-processes-after.txt`
  record exact-folder CIM results. Native windows returned empty. Own mock engine
  and fixture HTTP server stopped; own three scheduled tasks deleted. Fixture
  profiles, scripts and evidence retained for review and approved follow-up.
- Evidence manifest: `e2e-2026-09-06-evidence.tsv`. All54 top-level PNGs were
  opened and inspected; each is under200KB. No missing referenced image.

- Final REPORT sent once by AGB (`a988534b-1dd8-43a8-882d-43f8f8136236`).
  Native release succeeded; GUI FREE sent to surya-cef3 and jag-0906-osprey.
