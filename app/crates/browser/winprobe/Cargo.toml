[package]
name = "winprobe"
version = "0.0.0"
edition = "2024"

[workspace]

[dependencies]
futures = "0.3"

[target.'cfg(windows)'.dependencies]
windows = { version = "0.62", features = ["Win32_Foundation", "Win32_Security", "Win32_System_Threading", "Win32_Graphics_Gdi", "Win32_Media"] }
