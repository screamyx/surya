[package]
name = "surya-harness"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
surya-proto.workspace = true
base64 = "0.22"
reqwest.workspace = true
tokio.workspace = true
tokio-util.workspace = true
uuid.workspace = true
futures.workspace = true
async-trait.workspace = true
serde.workspace = true
serde_json.workspace = true
anyhow.workspace = true
thiserror.workspace = true
tracing.workspace = true

[target.'cfg(unix)'.dependencies]
libc.workspace = true

[dev-dependencies]
tempfile.workspace = true
surya-test-deadlines.workspace = true
# The auto-allow list is checked against the tools the sidecar actually
# advertises, so the two cannot drift apart. Test-only: nothing in the
# harness links the sidecar at runtime, it launches the binary.
surya-mcp.workspace = true
